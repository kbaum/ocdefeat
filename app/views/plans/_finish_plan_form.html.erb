<div class="form-check">
  <%= form_for(plan) do |f| %>
    <%= f.label :progress, "Mark this ERP plan as finished", class: "form-check-label" %>
    <%= f.check_box :progress, class: "form-check-input toggle", checked: plan.accomplished? %>
  <% end %>
</div>
