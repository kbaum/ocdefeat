<h3><em>Admin Access to All Accounts</em></h3>

<br>

<div>
  <%= form_tag "/users", method: "get" do %>
    <%= label_tag "role", "Filter Users by Role:" %><br>
    <%= select_tag "role", options_for_select(["patient", "therapist", "admin"]), prompt: "Select Role" %>
    <%= submit_tag "Filter Users" %>
  <% end %>
</div>

<br>

<%= render partial: "users/users_ul", locals: { users: filtered_users } %>
<!-- The partial app/views/users/_users_ul.html.erb uses the local variable users,
which is set = to the value of filtered_users local variable found here in app/views/filter_users/_admin.html.erb partial,
which, in turn, got its filtered_users local variable set = to the value of @filtered_users
in app/views/users/index.html.erb from users#index
The above render call works, but this works too:
<%= render partial: "users/users_ul", locals: { users: local_assigns[:filtered_users] } %> -->
<br>

<div class="well well-lg">
  <p class="unassigned"><strong>Users Awaiting Role Assignment:</strong></p>

  <br>

  <p class="unassigned"><em>Prospective Patients:</em></p>

  <%= render partial: "users/users_ul", locals: { users: prospective_patients } %>

  <p class="unassigned"><em>Therapists To-Be:</em></p>

  <%= render partial: "users/users_ul", locals: { users: therapists_to_be } %>

  <p class="unassigned"><em>Aspiring Admins:</em></p>

  <%= render partial: "users/users_ul", locals: { users: aspiring_admins } %>
</div>
