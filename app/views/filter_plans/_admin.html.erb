<%= form_tag(plans_path, method: "get") do %>
  <%= select_tag :date, options_for_select(["Today", "Before Today"]), prompt: "Date Designed" %>
  <%= submit_tag "Filter Plans" %>
<% end %>

<br>

<%= render partial: "plans/plans_ul", locals: { plans: plans } %>
