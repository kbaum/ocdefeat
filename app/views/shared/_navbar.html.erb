<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "OCDefeat", root_path, :class => "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <% if current_user.unassigned? %>
            <%= render 'shared/unassigned_nav_links' %>
          <% elsif current_user.patient? %>
            <%= render 'shared/patient_nav_links' %>
          <% elsif current_user.therapist? %>
            <%= render 'shared/therapist_nav_links' %>
          <% elsif current_user.admin? %>
            <%= render 'shared/admin_nav_links' %>
          <% end %>
          <% unless current_user.unassigned? %>
            <li><%= link_to "Obsessions Log", obsessions_path %></li>
            <li><%= link_to "Index of ERP Plans", plans_path %></li>
          <% end %>
          <li><%= link_to "Common OCD Themes", themes_path %></li>
          <li><%= link_to "Log Out", logout_path, method: :delete %></li>
        <% else %>
            <li class="active"><%= link_to "Sign Up", signup_path %></li>
            <li><%= link_to "Log In with OCDefeat", login_path %></li>
            <li><%= link_to "Log In with Twitter", "/auth/twitter" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
