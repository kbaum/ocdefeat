<% if stepless_patient %>
  <h4><%= stepless_patient.name %>'s Preliminary Plans (Sans Steps):</h4>
<% end %>

<ul>
  <% plans.each do |plan| %>
    <li><%= link_to plan.title, plan_path(plan) %></li>
  <% end %>
</ul>
